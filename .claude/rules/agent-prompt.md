---
paths:
  - "agents/**/*.md"
---

# 서브에이전트 CLAUDE.md 작성 규칙

- 한국어로 작성한다.
- 출력 형식은 반드시 JSON을 포함하며, 공통 필드 (`status`, `summary`, `details`, `confidence`)를 유지한다.
- 민감 정보(API 키, 비밀번호, 토큰)를 출력 예시에 포함하지 않는다.
- 역할, 입력, 작업 절차, 출력 형식 섹션을 반드시 포함한다.
- 에이전트별 고유 출력 필드가 있다면 공통 JSON 구조에 추가하는 형태로 정의한다.
